// Legacy bundle decommissioned during /v2 cutover (2026-05-21).
// Original archived at app.js.legacy. Empty stub kept so any stale SW caches
// referencing /assets/app.js receive a harmless 200 instead of a 404 fetch error.
